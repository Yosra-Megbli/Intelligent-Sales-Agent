# --- Phase 1: core / database ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3
redis==5.0.8
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0

pyyaml==6.0.2

# --- Phase 3A: LLM adapter ---
groq==0.11.0
httpx<0.28

# --- Later phases (kept here for reference, not required yet) ---
# langgraph
# langchain-core
# google-generativeai
# python-telegram-bot

# --- Phase 6: follow-up scheduling ---
# No scheduling library is required by this codebase itself; wire
# followup.scheduler.run_followup_cycle() into whatever your deployment
# already uses (cron, Celery beat, APScheduler...).
python-multipart==0.0.12
