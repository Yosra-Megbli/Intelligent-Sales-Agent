# --- Core / API / Database ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3
redis==5.0.8
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-multipart==0.0.12
pyyaml==6.0.2

# --- LLM Adapter & HTTP client ---
groq==0.11.0
httpx<0.28

# --- Contract & PDF Generation ---
reportlab>=4.0.0

# --- RAG v2: embeddings, vector storage, PDF text extraction ---
# google-generativeai / pgvector are optional at import time (see
# ai/providers/embeddings/google.py, database/vector_types.py) - only
# actually instantiating a real provider or running against PostgreSQL
# requires them, so `pytest tests/` still works without a Google AI key.
google-generativeai>=0.8.0
pgvector>=0.3.0
pypdf>=5.0.0
